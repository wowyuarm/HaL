[package]
name = "hal-desktop"
version = "0.1.0"
description = "Thin Tauri shell for HaL"
authors = ["Yu"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.5.6", features = [] }

[dependencies]
tauri = { version = "2.10.3", features = [] }
